<UserControl
  x:Class="LogGenius.Modules.Timeline.TrackView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:LogGenius.Modules.Timeline="clr-namespace:LogGenius.Modules.Timeline"
  mc:Ignorable="d"
  x:Name="THIS_TrackView"
  d:DesignHeight="450" d:DesignWidth="800">
  <UserControl.Resources>
    <Style x:Key="RecordMarkButtonStyle" TargetType="Button">
      <Setter Property="Width" Value="8"/>
      <Setter Property="Height" Value="8"/>
      <Setter Property="Background" Value="LightGray"/>
      <Setter Property="BorderBrush" Value="Gray"/>
      <Setter Property="ToolTip" Value="{Binding Value}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate>
            <Border
              CornerRadius="4"
              Background="{TemplateBinding Background}"
              BorderBrush="{TemplateBinding BorderBrush}"
              BorderThickness="2"
              />
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="White"/>
        </Trigger>
      </Style.Triggers>
    </Style>
  </UserControl.Resources>
  <Border
    BorderThickness="0,0,0,1"
    BorderBrush="{DynamicResource GridLinesBrush}"
    Height="{Binding HeaderHeight, ElementName=THIS_TrackView}"
    >
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="1*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <Grid Grid.Row="0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="{Binding HeaderWidth, RelativeSource={RelativeSource AncestorType=LogGenius.Modules.Timeline:TimelineView}}"/>
          <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Canvas
          Grid.Column="1"
          x:Name="PART_Canvas"
          ClipToBounds="True"
          />
        <Border
          Grid.Column="0"
          >
          <TextBlock
            Grid.Row="0"
            Text="{Binding Track.Identity.Name, ElementName=THIS_TrackView}"
            FontSize="{DynamicResource FontSize}"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            />
        </Border>
      </Grid>
      <Thumb
        Grid.Row="1"
        Cursor="SizeNS"
        DragDelta="OnVerticalThumbDragDelta"
        Height="5"
        Background="Transparent"
        BorderBrush="Transparent"
        Margin="0,0,0,-3"
        MouseDoubleClick="OnVerticalThumbMouseDoubleClicked"
        />
    </Grid>
  </Border>
</UserControl>
